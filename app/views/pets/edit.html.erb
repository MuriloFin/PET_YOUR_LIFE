<div class="container mt-4">
  <div class="row">
    <div class="col-6 m-auto">
      <div class="pet-form">
        <h1><%= params[:adopted] == "true" ? "Edit Pet"%></h1>
        <div class="mt-3">
          <%= render "form", pet: @pet %>
        </div>
      </div>
    </div>
  </div>
</div>
